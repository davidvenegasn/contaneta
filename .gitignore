.DS_Store

# --- Python / venv / caches ---
.venv/
venv/
__pycache__/
*.py[cod]

# --- Logs / backups / residuos ---
*.log
*.bak
*.bad

# --- SQLite local (NO versionar) ---
invoicing.db
invoicing.db-*
invoicing.db.*
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
sqlite_aux_backup/

# Old DB recovery artifacts — do not commit
dump_recover.sql
fixed.db

# --- Storage local (NO versionar) ---
storage/**
!storage/
!storage/.gitkeep
!storage/README.md

# --- Keys/certs (NUNCA versionar) ---
keys/**
!keys/
!keys/README.md

# --- Backups locales (NO versionar) ---
backup/
backups/

# --- Secrets ---
.env

# Snapshot de respaldo: no editar ni commitear (ver docs/GUARDRAILS.md)
_snapshot_before_mindtrip/

# Bank statement & invoice samples for parser development (public PDFs from transparency portals)
# Not committed to keep repo small; re-download via samples/*/README.md
samples/bank_statements/**/*.pdf
samples/invoices/**/*.pdf

# Context artifacts — keep structure, ignore content
# Comment out the next line if you WANT to commit workflow artifacts
# context/**/*.md
# !context/**/README.md
# !context/**/.gitkeep
